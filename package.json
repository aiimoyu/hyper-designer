{
  "name": "hyper-designer",
  "version": "1.0.0",
  "description": "OpenCode plugin that implements specialized agents and workflow management for requirements engineering and system design",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.1.60",
    "@types/node": "^20.0.0",
    "@vitest/coverage-v8": "^1.6.1",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  },
  "optionalDependencies": {
    "@rollup/rollup-win32-x64-msvc": "*"
  },
  "dependencies": {
    "diff": "^8.0.3"
  }
}
