---
name: sdd-plan-generator
description: >
  根据模块功能设计说明书（MFD）和现有代码，生成规格驱动开发（SDD）的详细开发计划。
  当用户提到"模块功能设计"、"开发计划"、"SDD"、"任务拆分"、"subagent 任务"、"把设计转成代码任务"时，必须使用此 skill。
  输出包含：任务波次划分、复杂度评级、子 agent 分发策略、接口卡片、验收标准和 TDD 测试场景。
  即使用户只说"帮我拆任务"或"按设计文档写计划"也应触发此 skill。
---

# SDD 开发计划生成器

基于**模块功能设计说明书（Module Functional Design，MFD）**和已有代码，生成可直接分发给 vibe-coding subagent 执行的开发计划。

---

## PHASE 1：信息收集访谈（默认入口）

**你是一名规格顾问，先访谈，再出计划。**

### Step 0：分类判断

收到请求后，立即判断用户处于哪个阶段：

| 场景                    | 响应策略                               |
| ----------------------- | -------------------------------------- |
| 用户直接粘贴了 MFD 文档 | 跳过大部分访谈，快速确认几个关键决策点 |
| 用户有文档但未粘贴      | 请用户提供，同时询问技术栈             |
| 用户只描述了模块        | 全流程访谈，重建关键设计决策           |

### Step 1：必要信息收集

**每次访谈轮次结束前，运行清单自检：**

```
访谈完整性清单（全部 YES 才进入计划生成）：
□ MFD 文档（或等价描述）已获取？
□ 技术栈和架构元素已确认？
□ 现有代码结构已了解（或确认是 greenfield）？
□ 测试策略已决策（TDD / 测试在后 / 无测试）？
□ 目标 subagent 能力已了解（单 agent 还是多 agent 并行）？
□ 计划粒度已确认（模块级 / 功能级 / 接口级）？
```

**必问核心问题（按优先级，通过提问工具一次性呈现）：**

1. **设计文档**："请提供模块功能设计说明书，或描述该模块的核心功能列表和接口诉求。"
2. **技术栈**："这个模块用什么语言/框架？主要依赖库是什么？"
3. **代码现状**："是全新模块，还是在现有代码上扩展？如有现有代码，请提供目录结构或关键文件片段。"
4. **测试策略**："是否需要自动化测试？TDD（先写测试）还是实现后补测试？"
5. **并行度**："计划是给单个 agent 顺序执行，还是多个 subagent 并行？"

---

## PHASE 2：计划生成

访谈完整性全部满足后，生成开发计划。

### 计划生成原则

#### 原则 0：计划划分与排序（最高优先级）

**开发计划必须按照依赖关系和技术栈层次进行排序，而非机械按 AR 或模块划分。**

##### 0.1 框架优先，渐进细化

对于全新项目（greenfield），必须遵循以下顺序：

```
Wave 0（基础设施层）：
├── 项目脚手架初始化
├── 构建工具配置（tsconfig, vite/webpack, eslint 等）
├── 目录结构创建
├── 基础类型定义
└── 核心依赖安装

Wave 1（数据与接口层）：
├── 数据模型/Schema 定义
├── 接口类型声明
├── 基础工具函数
└── 配置文件

Wave 2（核心业务层）：
├── 核心业务逻辑实现
├── 服务层实现
└── 业务规则处理

Wave 3（接口与集成层）：
├── API 端点实现
├── UI 组件实现
└── 模块间集成

Wave FINAL（验收层）：
├── 集成测试
├── 端到端测试
└── 需求覆盖审查
```

**对于增量项目（brownfield）**，Wave 0 可能已存在，但仍需确认基础设施是否需要扩展。

##### 0.2 工作量适中，避免极端

**开发计划数量建议**：

| 项目规模 | 建议计划数量 | 单个计划工作量 |
|----------|--------------|----------------|
| 小型（单模块增强） | 1 个 | 1-3 人日 |
| 中型（多模块） | 2 个 | 2-5 人日 |
| 大型（全新系统） | 3 个 | 3-7 人日 |

**核心约束**：**一个开发计划最多 3 个**，超过 3 个应合并或重新评估范围。

**避免极端**：

- ❌ 过少：一个计划包含 10+ 个 AR，工作量超过 2 周
- ❌ 过多：每个 AR 单独一个计划，导致计划碎片化
- ✅ 适中：每个计划包含 2-4 个相关 AR，工作量 2-5 人日

##### 0.3 耦合度优先于 AR 边界

**不要机械按 AR 划分计划，要考虑功能耦合度。**

**合并规则**（应该合并为一个计划）：

- 多个 AR 属于同一模块且共享数据模型
- 多个 AR 有强依赖关系，分开实现会导致反复修改
- 多个 AR 的实现涉及同一组文件
- 多个 AR 共同完成一个完整的用户场景

**拆分规则**（应该拆分为多个计划）：

- AR 涉及完全独立的模块
- AR 可以独立测试和验收
- AR 的实现不需要等待其他 AR 完成
- 单个计划工作量超过 5 人日

##### 0.4 全新项目的框架构建计划

**对于全新项目，第一个计划必须是框架构建计划。**

框架构建计划必须包含：

```
## 框架构建计划（Wave 0）

**目标**：搭建项目骨架，为后续开发提供基础支撑。

**必须包含**：
1. 项目初始化
   - 包管理器配置（package.json / pnpm-workspace.yaml）
   - TypeScript 配置（tsconfig.json, tsconfig.build.json）
   - 构建工具配置（vite.config.ts / webpack.config.ts）
   - 代码规范配置（eslint, prettier）

2. 目录结构
   - src/
   - tests/
   - docs/
   - 配置文件目录

3. 基础依赖
   - 运行时依赖
   - 开发依赖
   - 类型定义

4. 开发脚本
   - npm run dev
   - npm run build
   - npm run test
   - npm run lint

**验收标准**：
- [ ] 项目可成功编译（npm run build）
- [ ] 测试框架可运行（npm run test）
- [ ] 代码规范检查通过（npm run lint）
- [ ] 开发服务器可启动（npm run dev）
```

**框架构建计划完成后，才能开始业务功能计划。**

#### 原则 1：规格完整性覆盖

每个 MFD 中的功能（3.x 章节）必须对应至少一个开发任务。
每个 MFD 中的接口卡片必须对应至少一个实现任务。
每个 SR（系统需求）→ AR（分配需求）映射必须有可验证的验收标准。

#### 原则 2：任务粒度规则

- **一个任务 = 一个关注点 = 1-4 个文件**
- 触碰 5+ 文件或 2+ 不相关关注点 → 拆分任务
- 目标：每个波次 3-7 个可并行任务
- 单个任务执行时间：15-60 分钟

#### 原则 3：复杂度评级

每个任务必须标注复杂度：

| 级别            | 说明                      | 示例                            |
| --------------- | ------------------------- | ------------------------------- |
| `[quick]`       | 简单配置/脚手架，< 15 min | 创建文件夹结构、类型定义        |
| `[medium]`      | 标准实现，15-30 min       | 实现一个接口端点                |
| `[deep]`        | 复杂逻辑/算法，30-60 min  | 状态机、并发处理、FMEA 缓解措施 |
| `[integration]` | 跨模块集成，需要协调      | 端到端流程串联                  |
| `[review]`      | 代码审查/测试覆盖验证     | 最终波次                        |

#### 原则 4：依赖波次划分

```
Wave 1（立即开始 — 基础层）：
  类型定义、数据模型、接口协议、脚手架

Wave 2（Wave 1 完成后 — 核心实现，最大并行）：
  各功能的核心逻辑，互相独立

Wave 3（Wave 2 完成后 — 集成层）：
  跨功能集成、错误处理、DFX 措施落地

Wave FINAL（所有任务完成后 — 验收）：
  集成测试、接口联调验证、需求覆盖审查
```

---

## 计划输出格式

输出到 `dev-plan/{模块名}-dev-plan.md`，结构如下：

````markdown
# {模块名} 开发计划

> **基于**：{MFD 文档版本/描述}
> **技术栈**：{语言、框架、关键依赖}
> **测试策略**：{TDD / 测试在后 / 无测试}
> **并行模式**：{单 agent 顺序 / 多 agent 波次并行}

---

## TL;DR

**目标**：[一句话描述]
**交付件**：
- [具体文件/端点/功能]

**估算工作量**：[快速 / 短 / 中 / 长]
**关键路径**：任务 1 → 任务 N → 任务 Final
**并行加速**：~XX% 比顺序执行更快

---

## 功能覆盖矩阵

| MFD 功能 | 对应任务 | SR 编号 | 验收标准 |
|---------|---------|---------|---------|
| F-001 {功能名} | T-03, T-07 | SR-001 | [具体可验证条件] |

---

## 执行波次

```
Wave 1（基础层，立即并行）：
├── T-01: 数据模型定义 [quick]
├── T-02: 接口类型声明 [quick]
└── T-03: 配置和常量 [quick]

Wave 2（核心实现，最大并行）：
├── T-04: {功能A} 实现（依赖: T-01, T-02）[medium/deep]
├── T-05: {功能B} 实现（依赖: T-01, T-02）[medium/deep]
└── T-06: {接口X} 实现（依赖: T-02）[medium]

Wave 3（集成层）：
├── T-07: 端到端流程集成（依赖: T-04, T-05）[integration]
└── T-08: DFX 措施实现（依赖: T-06, T-07）[medium]

Wave FINAL（验收）：
├── T-F1: 集成测试执行 [review]
└── T-F2: AR 覆盖审查 [review]
```

---

## 任务详情

### T-01：{任务标题}

**关联 MFD**：§3.x {功能名}
**关联 SR**：SR-001, SR-002
**复杂度**：[quick/medium/deep/integration/review]
**推荐 Agent 能力**：{描述所需能力，如"熟悉 TypeScript 类型系统"}

**要做什么**：
- [明确的实现步骤]
- [需要覆盖的边界情况]

**不要做什么**：
- [明确排除项，防止 scope creep]
- [避免哪些技术债务写法]

**参考资料**：
- `src/existing/module.ts:45-78` — 参考此处的错误处理模式
- MFD §3.x 接口设计表格 — 实现此接口诉求
- [外部库文档链接]

**并行信息**：
- 可并行运行：YES / NO
- 所在波次：Wave N（与 T-XX, T-YY 并行）
- 阻塞任务：T-04, T-05（依赖本任务完成）
- 被阻塞于：无（可立即开始）

**验收标准**：

若 TDD 模式：
- [ ] 测试文件已创建：`tests/module/feature.test.ts`
- [ ] 运行 `{测试命令}` → PASS（N 个测试，0 失败）

QA 场景（必填，任务不完整则拒绝）：

```
场景：正常路径 — {描述期望成功的操作}
  工具：[curl / 单元测试 / Playwright]
  前置条件：[具体初始状态]
  步骤：
    1. [具体操作，含确切命令/选择器/端点]
    2. [下一步操作及中间状态]
    3. [断言 — 具体期望值]
  预期结果：[可观测的、二元的通过/失败]
  证据：dev-plan/evidence/T-01-happy.{ext}

场景：异常路径 — {描述应优雅失败的操作}
  工具：[同上]
  前置条件：[无效输入/缺少依赖]
  步骤：
    1. [触发错误条件]
    2. [断言错误被正确处理]
  预期结果：[带正确错误码/消息的优雅失败]
  证据：dev-plan/evidence/T-01-error.{ext}
```

**接口卡片**（如适用，来自 MFD §接口设计）：

| 项目 | 内容 |
|------|------|
| 接口名称 | {从 MFD 提取} |
| 接口类型 | REST / gRPC / 消息队列 / 事件 |
| 输入参数 | {从 MFD 提取} |
| 输出参数 | {从 MFD 提取} |
| SLA | {响应时间 / 吞吐量} |
| 幂等性 | YES / NO |

**分配需求（AR）**：

| SR 编号 | SR 描述 | AR 编号 | AR 描述 | 系统元素 |
|---------|---------|---------|---------|---------|
| SR-001 | ... | AR-001 | ... | {实现文件/组件} |

**提交**：YES（与 T-0X 组合提交）
- 提交信息：`feat(module): 描述`
- 文件：`path/to/file.ts`

---

[重复以上结构 for 每个任务]

---

## 最终验收波次

### T-F1：集成测试执行

**要做什么**：
1. 按 MFD §3.x 每个功能执行端到端流程
2. 验证所有 SR→AR 映射的分配需求均可测通过
3. 执行 DFX 分析中的 FMEA 缓解措施验证
4. 保存所有证据到 `dev-plan/evidence/final/`

**验收标准**：
- [ ] 所有 QA 场景证据文件存在
- [ ] AR 覆盖率 = 100%（无遗漏需求）
- [ ] FMEA 缓解措施均有对应测试验证

### T-F2：AR 覆盖审查

**要做什么**：
1. 逐行对照 MFD §分配需求表
2. 确认每个 AR 有对应实现代码
3. 确认每个 SR 可追溯到至少一个 AR
4. 输出覆盖报告

---

## 成功标准

```bash
# 运行全量测试
{测试命令}  # 预期：全部通过

# 检查 AR 覆盖
# 预期：MFD 中所有 SR 均有对应 AR 和实现
```

- [ ] 所有"必须有"项均已实现
- [ ] 所有"不能有"项均未引入
- [ ] 所有测试通过
- [ ] 接口联调验证通过
````

---

## 访谈反模式（避免这些）

**NEVER：**

- 在访谈阶段生成计划文件
- 跳过技术栈确认直接生成
- 生成没有 QA 场景的任务（不完整）
- 让任务触碰 5+ 无关文件而不拆分

**ALWAYS：**

- 对每个 MFD 接口卡片生成对应的实现任务
- 将 FMEA 缓解措施显式列为任务（不是注释）
- 标注每个任务的复杂度等级
- 确保 Wave 1 是纯基础层（无业务逻辑）

---

## 与 MFD 的映射关系

| MFD 章节              | 计划对应                       |
| --------------------- | ------------------------------ |
| §2.1 设计原则与约束   | 全局"不要做什么"规则           |
| §2.2 总体实现思路     | Wave 1 架构决策任务            |
| §2.3 领域数据模型     | Wave 1 数据模型任务（[quick]） |
| §2.4 周边系统元素关系 | 接口适配层任务                 |
| §3.x 功能实现设计     | Wave 2 核心实现任务            |
| §3.x 接口设计         | 每个接口卡片 → 独立实现任务    |
| §3.x DFX 分析         | Wave 3 可靠性/安全措施任务     |
| §3.x 分配需求 SR→AR   | 验收标准 + T-F2 AR 覆盖审查    |

---

## 快速参考：复杂度判断

```
是否涉及状态机或复杂算法？       → [deep]
是否是跨 Wave 2 多模块集成？     → [integration]
是否是单接口实现 + 单元测试？    → [medium]
是否只是类型定义/配置/脚手架？   → [quick]
是否是执行已有测试 + 审查报告？  → [review]
```
