# Phase 3 — SDD 开发计划

## 目标

将需求设计说明书中的 AR 分配拆解为**可直接执行的开发任务**，生成结构化的开发计划，使开发 Agent（如 Claude Code）能够无歧义地理解任务内容、执行顺序和验收条件。

---

## 前置条件

- 已完成 Phase 2，需求设计说明书已确认
- 所有 AR 的功能点已明确
- 修改围栏已确认

---

## 代码阅读要求（必须）

**在生成SDD计划前，必须阅读相关代码文件，明确修改位置。**

### 阅读步骤

1. **阅读需求设计说明书中的修改围栏**：确认允许修改和禁止修改的范围
2. **阅读代码库**：定位要修改的文件和位置
3. **记录修改位置**：每个任务必须明确以下信息

### 任务修改信息格式

每个开发任务必须包含：

```
**修改文件**：
- 文件路径：[具体路径，如 src/services/feedback/index.ts]
- 修改位置：[行号范围或函数名，如 L45-67 或 submitFeedback 函数]
- 修改类型：[新增 / 修改 / 删除]
- 修改内容：[具体描述，如"新增 submitFeedback 方法，处理用户反馈提交"]

**新增文件**（如有）：
- 文件路径：[具体路径]
- 依赖关系：[依赖哪些现有模块]
- 集成方式：[如何与现有代码集成]
```

---

## 输出：开发计划（Spec Plan）

### 文档结构

```
开发计划
├── §0 概要（任务总览）
├── §1 执行前准备
│   ├── 1.1 环境与依赖确认
│   └── 1.2 代码阅读清单
├── §2 开发任务列表（按里程碑分组）
│   └── 每个任务：描述 / AR 映射 / 功能点 / 实现要点 / 验收条件
├── §3 任务依赖与执行顺序
├── §4 测试计划
├── §5 风险与注意事项
└── §6 给开发 Agent 的执行说明
```

---

## 各节写作指南

### §0 概要

| 项目 | 内容 |
|------|------|
| **需求来源** | [需求分析说明书名称] |
| **设计来源** | [需求设计说明书名称] |
| **任务总数** | [N 个开发任务] |
| **预估工作量** | [N 人日 / N 人周] |
| **里程碑** | M1: [日期] / M2: [日期] / M3: [日期] |

**本次开发摘要**（2-3 句话）：
[描述本次开发的整体任务范围和核心工作。如："本次开发包含 3 个新模块和 2 个现有模块的扩展，核心是为 Skill 生成流程增加用户反馈回路和知识融合能力，共 8 个开发任务，预计 4 周完成。"]

---

### §1 执行前准备

#### 1.1 环境与依赖确认

在开始编码前，确认以下准备工作（Agent 需逐项检查）：

```
□ 开发分支已从主干创建（分支名建议：feature/[需求名]-[日期]）
□ 依赖已安装/引入：
  - [依赖 1]：[版本] — [用途]
  - [依赖 2]：[版本] — [用途]
□ 数据库迁移脚本执行权限已确认（如有 schema 变更）
□ 外部服务/API 访问权限已确认（如有）
□ [其他环境前置条件]
```

#### 1.2 代码阅读清单

在修改代码前，必须阅读以下现有代码，理解当前实现（来自修改围栏分析）：

```
□ [文件/目录路径 1]：了解 [现有模块的接口和职责]
□ [文件/目录路径 2]：了解 [现有数据模型和存储方式]
□ [文件/目录路径 3]：了解 [认证/权限中间件的使用方式]
□ [文件/目录路径 4]：了解 [现有的错误处理约定]
```

---

### §2 开发任务列表

**任务来源**：每个任务对应一个或多个 AR，AR 的功能点直接拆解为任务的实现要点。

任务编号格式：`T-{里程碑编号}{序号:02d}`，如 `T-101`（M1 第 1 个任务）。

---

#### 里程碑 M1：[M1 名称]（目标日期：[日期]）

---

##### 任务 T-101：[任务名称]

| 项目 | 内容 |
|------|------|
| **对应 AR** | AR-001-01 — [AR 名称] |
| **系统元素** | [模块名]（新增 / 扩展） |
| **预估工作量** | [N 人日] |
| **依赖任务** | 无 / T-xxx |

**任务描述**：
[2-3 句话，从实现者视角说明要做什么。明确是新增文件、修改哪个文件、添加什么功能。]

**功能点清单**（直接来自 AR 功能点）：
- [ ] [功能点 1]
- [ ] [功能点 2]
- [ ] [功能点 3]

**实现要点**：
1. [关键步骤 1，如"在 [文件] 中新增 [类/方法]，实现 [逻辑]"]
2. [关键步骤 2]
3. [需要特别注意的实现细节，如"注意与现有 [模块] 的兼容性"]

**输出物**：
- `[新增/修改的文件路径]`：[说明]
- `[测试文件路径]`：单元测试

**验收条件**：
- [ ] [具体可客观判断的条件 1，如"提交反馈后，数据库中 feedback 表新增对应记录"]
- [ ] [条件 2，如"接口响应时间 < 500ms（本地环境）"]
- [ ] 功能点清单中所有条目全部完成
- [ ] 单元测试全部通过，覆盖正常/异常两种路径

---

##### 任务 T-102：[任务名称]

| 项目 | 内容 |
|------|------|
| **对应 AR** | AR-001-02 — [AR 名称] |
| **系统元素** | [模块名]（新增 / 扩展） |
| **预估工作量** | [N 人日] |
| **依赖任务** | T-101（需先完成） |

**任务描述**：[描述]

**功能点清单**：
- [ ] [功能点 1]
- [ ] [功能点 2]

**实现要点**：
1. [步骤]

**验收条件**：
- [ ] [条件]
- [ ] 单元测试全部通过

---

#### 里程碑 M2：[M2 名称]（目标日期：[日期]）

[继续按相同格式列出 M2 的任务]

---

#### 里程碑 M3：[M3 名称]（目标日期：[日期]）

[继续按相同格式列出 M3 的任务]

---

### §3 任务依赖与执行顺序

```
[数据层/存储任务]
T-101 → [数据模型/Schema]
  └→ T-102 → [核心服务逻辑]
       └→ T-103 → [接口层/控制器]
                  └→ T-104 → [UI 层/前端]（如有）

[可并行执行的任务]
T-201（独立模块）──┐
T-202（独立模块）──┴→ T-203（集成测试，依赖以上全部完成）
```

**并行建议**：
- [T-xxx 和 T-xxx] 无依赖关系，可分配给不同成员并行
- [T-xxx] 强依赖 [T-xxx]，必须串行等待

**关键路径**：T-xxx → T-xxx → T-xxx（影响 M2 交付的最长路径）

---

### §4 测试计划

#### 4.1 单元测试要求

每个任务交付时，必须同时提交对应的单元测试：

| 任务 | 测试文件位置 | 覆盖重点 |
|------|------------|----------|
| T-101 | `[路径]` | [核心逻辑/边界条件/异常路径] |
| T-102 | `[路径]` | [核心逻辑] |

#### 4.2 集成测试用例

| 用例编号 | 覆盖场景 | 对应主成功场景 | 验收标准 |
|----------|----------|--------------|----------|
| TC-01 | [主流程] | §3.2 主成功场景 | [通过条件] |
| TC-02 | [异常流程：用户取消] | — | [正确回滚/提示] |
| TC-03 | [性能场景] | — | [响应时间 < Ns] |

#### 4.3 回归测试范围

由于本次修改涉及以下现有模块，需进行回归测试：

| 模块 | 测试重点 | 测试方法 |
|------|----------|----------|
| [现有模块 1] | [关键功能点] | [自动化测试/手动验证] |
| [现有模块 2] | [关键功能点] | [方法] |

---

### §5 风险与注意事项

#### 5.1 开发注意事项

```
⚠️ 修改围栏提醒（来自需求设计说明书）：
  禁止修改：
  - [路径]（原因：[说明]）
  - [路径]（原因：[说明]）

📝 编码规范：
  - [命名规范/文件组织规范，与现有代码保持一致]
  - [注释要求]

🔑 关键实现注意：
  - [容易踩坑的地方]
  - [需要特别注意的边界条件或并发问题]
```

#### 5.2 风险清单

| 风险 | 概率 | 影响 | 缓解措施 | 预案 |
|------|------|------|----------|------|
| [技术风险：如某依赖不兼容] | 中 | 高 | [提前调研] | [备选方案] |
| [依赖风险：如外部 API 不稳定] | 低 | 中 | [增加重试/降级] | [本地缓存] |
| [进度风险：如 T-xxx 复杂度超预期] | 中 | 中 | [提前 spike] | [缩减功能点范围] |

---

### §6 给开发 Agent 的执行说明

> 以下说明直接面向 Claude Code 等开发 Agent：

**开始前必读**：
1. 读取需求设计说明书的"§约束：修改围栏"，确认可操作的代码范围
2. 按"§1.2 代码阅读清单"阅读现有代码，理解当前实现方式
3. 确认"§1.1 环境与依赖确认"中的所有前置条件已满足

**执行规范**：
- 按任务依赖顺序执行，不要跳过有前置依赖的任务
- 每完成一个任务，运行对应单元测试，全部通过后再继续下一个
- **遇到与设计不符的情况（如现有接口签名不一致、字段缺失等），先暂停并报告，不要自行修改设计方案**
- 新增代码的命名、注释风格遵循现有代码约定

**完成标志**：
- [ ] 所有任务的功能点清单全部勾选完成
- [ ] 所有单元测试通过
- [ ] 集成测试用例全部通过
- [ ] 回归测试无新增失败用例
- [ ] 代码 Review 通过（如有要求）

---

## 质量自检

- [ ] 每个 AR 都对应至少一个开发任务
- [ ] 每个任务的功能点清单直接来自 AR 功能点
- [ ] 每个任务有可客观判断的验收条件（不依赖主观判断）
- [ ] 任务依赖关系已完整标注
- [ ] 修改围栏已在执行说明中体现
- [ ] 测试计划覆盖主成功场景和关键异常场景
- [ ] 每个任务明确了修改文件路径、位置和内容
