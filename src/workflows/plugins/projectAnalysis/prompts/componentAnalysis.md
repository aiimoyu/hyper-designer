## Current Phase: Component Analysis

```xml
<workflow_context>
  <pipeline>
    <curr_stage id="componentAnalysis"/>
    <pre_stage>systemAnalysis</pre_stage>
  </pipeline>
  <executing_agent>HAnalysis</executing_agent>
  <core_objective>
    Analyze each component in parallel using the component manifest from Stage 1,
    produce per-component markdown and JSON outputs, and aggregate results for reconciliation.
  </core_objective>
</workflow_context>
```

### 1. Execution Standards

**Core Skills**:
- `project-analysis-concepts` - Shared concepts, terminology, and artifact contracts
- `component-analysis` - Manifest-driven fan-out orchestration and per-component analysis methodology

Load both skills to access:
- Shared terminology and artifact locations
- Manifest-driven orchestration constraints
- Component analysis dimensions (4: IOP, Responsibility/Scope, Interface Contracts, Internal Structure)
- Fan-out input/output contracts and retry semantics
- Reconciliation step requirements

**Critical Constraint**: `_meta/component-manifest.json` is the ONLY source of truth for component discovery in Stage 2.

STRICTLY FORBIDDEN:
- Do NOT rescan the project directory to discover components
- Do NOT use directory heuristics or file patterns to identify components
- Do NOT override the manifest with any alternative discovery method
- The manifest from Stage 1 defines the complete and authoritative component set

**Core Reference**: `.hyper-designer/projectAnalysis/_meta/component-manifest.json`
Generated by Stage 1 - contains the complete list of components to analyze with metadata.

---

### 2. Deliverables

| File | Path | Format Requirements |
|------|------|---------------------|
| **Per-Component Markdown** | `.hyper-designer/projectAnalysis/component/{componentSlug}.md` | Markdown, covers all 4 component dimensions, includes code references and Mermaid diagrams |
| **Per-Component Metadata** | `.hyper-designer/projectAnalysis/_meta/components/{componentSlug}.json` | JSON, machine-readable component contract with interfaces, dependencies, coverage metrics |
| **Component Analysis Summary** | `.hyper-designer/projectAnalysis/_meta/component-analysis-summary.json` | JSON, aggregates success/failure status, quality score, and reconciliation issues |

**Per-Component Outputs Required for Each Component in Manifest**:

For each `componentSlug` in `component-manifest.json`, produce:

1. **Component markdown**: `component/{componentSlug}.md`
   - Human-readable component analysis
   - Covers all 4 component dimensions:
     - Input/Output/Position (IOP)
     - Responsibility and Scope
     - Interface Contracts
     - Internal Structure and Patterns
   - Includes code references, Mermaid diagrams, and cross-references

2. **Component metadata**: `_meta/components/{componentSlug}.json`
   - Machine-readable component contract
   - Structured data for downstream validation and reconciliation
   - Includes: interfaces, dependencies, coverage metrics, quality indicators

**Aggregation Output**:

- **Component analysis summary**: `_meta/component-analysis-summary.json`
  - Lists all components analyzed
  - Tracks success/failure status per component
  - Provides reconciliation input for Stage 3

---

### 3. Fan-Out Orchestration

**Concurrency Control**:
- Max concurrent components: 5
- Dispatch strategy: Parallel fan-out with controlled concurrency
- Ordering: No dependency-based ordering required (manifest defines dependencies)
- Isolation: Each component analysis runs in isolated context

**Retry and Recovery**:

| Failure Type | Retry Strategy | Max Attempts | Recovery Action |
|--------------|----------------|---------------|-----------------|
| Transient error (network, timeout) | Exponential backoff | 3 | Retry component analysis |
| Component not found in manifest | No retry | 0 | Fail fast, log error |
| Missing required manifest field | No retry | 0 | Fail fast, log validation error |
| Analysis timeout (>5 min) | No retry | 0 | Mark as failed, continue with others |
| Output write failure | No retry | 0 | Mark as failed, preserve in-memory result |

**Failure Semantics**:

- Partial success allowed: Continue processing other components if some fail
- Failure isolation: One component failure does not abort Stage 2
- Failure tracking: Record failure reason and component in summary
- Reconciliation target: Failed components are flagged for Stage 3 coverage check

---

### 4. Reconciliation Step

After all component analyses complete, perform reconciliation:

1. **Compare manifest vs. outputs**
   - Verify every component in manifest has corresponding markdown and JSON outputs
   - Flag missing outputs as failures

2. **Validate cross-references**
   - Check that component dependencies reference valid componentSlugs
   - Verify Mermaid diagram nodes match manifest entries
   - Validate code references point to existing files

3. **Aggregate quality metrics**
   - Collect coverage percentages from all component JSON outputs
   - Compute overall component analysis health score
   - Identify components with low coverage or quality issues

4. **Produce summary**
   - Write `_meta/component-analysis-summary.json` with:
     - `totalComponents`: count from manifest
     - `successfulComponents`: count with valid outputs
     - `failedComponents`: list of componentSlugs that failed
     - `qualityScore`: aggregated metric
     - `reconciliationIssues`: list of cross-reference problems

---

### 5. Quality Checklist

Before completing Stage 2, verify:

| Requirement | Verification |
|-------------|--------------|
| Read component-manifest.json as ONLY source | Checked manifest path, did not scan project directory |
| No project rediscovery | Confirmed no directory scanning or file pattern matching |
| Every component has both outputs | Verified markdown and JSON exist for each componentSlug |
| All 4 dimensions covered | Each component analysis includes IOP, Responsibility/Scope, Interface Contracts, Internal Structure |
| Component JSON is valid | All `_meta/components/{componentSlug}.json` files parse correctly |
| Component markdown has references | Code citations and Mermaid diagrams present |
| Reconciliation summary written | `_meta/component-analysis-summary.json` exists and is valid |
| Cross-references validated | Dependencies, diagram nodes, and code paths all resolve |
| Failed components tracked | Summary includes `failedComponents` list if any failures |
| Concurrency limits respected | Max 5 concurrent analyses enforced |
