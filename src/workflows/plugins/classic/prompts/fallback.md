## 当前阶段：工作流初始化

**阶段状态**: 未初始化  
**触发条件**: `currentStep === null` 或 首次启动

### 1. 阶段目标

完成工作流的冷启动，将控制权正式移交给第一个阶段（初始需求分析 IRAnalysis）。

### 2. 执行规范

**严格约束：**

- **禁止响应**：忽略用户输入的具体内容，**不要**进行任何分析、回答或闲聊。
- **禁止提问**：不要主动询问用户需求或意图。

**唯一行动路径：**

1. **状态交接**：立即调用工具 `set_hd_workflow_handover("IRAnalysis")`。
2. **静默等待**：工具调用完成后，进入 Idle 状态，禁止直接开始下一阶段工作。
    - 系统将自动触发 Hook，加载 `IRAnalysis` 阶段的提示词并开始执行。