# 当前阶段

**你现在处于：用例分析 (Use Case Analysis) 阶段**

**阶段信息**

- 目的：将场景库中的功能场景细化为可执行、可验收的用例规格，明确输入/输出与验收标准，作为功能实现和测试的契约。
- 负责人：@HArchitect
- 要求技能：`use-case-analysis`
- 交付节拍：每个功能在完成后产出单文件 `{功能名}用例.md`

**必须使用 Skill：`use-case-analysis`**

使用此 skill 将场景细化为详细的用例规格，定义用例的前置条件、成功保证、触发事件、Actor、主成功场景、扩展场景和DFX属性。

## 所需资料

| 资料类别 | 具体资料 | 用途 | 必需性 |
|---------|---------|------|-------|
| 系统需求分析资料 | 场景库 | 细化主场景、备选场景、异常场景 | 必需（前阶段输出） |
| 系统需求分析资料 | FMEA库 | 故障模式、异常处理场景 | 可选 |
| 系统需求分析资料 | 功能库 | 功能规格说明 | 参考用 |

资料收集执行：读取 scenarioAnalysis 阶段输出的场景文档，询问历史故障记录，收集功能规格参考。

## 阶段输出

- 每个功能对应一个文件：`{功能名}用例.md`

## 使用技能

- `use-case-analysis`

## 质量审核要求

**强制审核流程：**

1. **生成文档**：完成本阶段所有输出文档
2. **立即触发HCritic审查**：文档生成后，**立即自动**使用 `HCritic` agent 进行设计审核
   - 调用方式：{{TOOL:delegate_critic_review}}
   - **不要等待用户确认，直接触发审查**
3. **处理审查结果**：
   - 审核未通过时：根据 HCritic 的反馈意见，**必须**修改文档并重新提交审查（返回步骤2）
   - 审核通过时：向用户确认"该阶段工作已完成，HCritic审核通过。是否进入下一阶段？"
4. **用户确认后**：执行 workflow 工具进入下一阶段
5. **禁止跳过审核**：未经 HCritic 审核通过的文档不得进入下一阶段

## 审查要点

- 用例规格完整性（前置条件、触发事件、Actor、主成功场景、扩展/异常场景完整且无歧义）
- 输入/输出明确性（数据格式、类型、边界条件、错误代码/异常分支）
- 验收标准可测试性（验收条件应能以自动化或手工测试判定通过/失败）

(End of file - updated)
