修改记录：更新 HArchitect 步骤流程为 8 步，新增资料收集步骤

- 变更概述：在原有 7 步流程中插入新的步骤2（收集本阶段所需资料），并将原步骤2-7顺序后移为步骤3-8；更新所有交叉引用与文末提醒。
- 主要修改点：
  - 概述中的步骤列表改为 8 步并包含 document-collector 提示
  - 在步骤1后插入详细的步骤2内容（收集资料，包括 manifest.md、draft.md、使用 explore/librarian/webfetch）
  - 将原有步骤标题和内部引用重新编号并调整返回路径（如审查不通过返回步骤4）
  - 更新文末提醒为“8步流程”

注意事项 / 经验：
- 保持原有步骤文字的语义不变，仅对编号与少量引用文本进行替换，避免破坏上游流程自动解析器对特定字符串的依赖。
- 文档中存在对步骤号的硬编码引用（如“返回步骤3”），必须一一校对并更新为新的编号，否则会引导错误工作流。
- lsp_diagnostics 无法检查 .md 文件（未配置 md LSP），已手动 grep 验证关键字符串。

后续建议：
- 若工作流解析器依赖具体文本标识，建议改用机器可解析的元数据（YAML frontmatter 或 JSON manifest）来表示步骤序列，以减少手工更新风险。
